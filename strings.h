//PROYECTO EDA

#ifndef FINALANGELEDA_STRINGS_H
#define FINALANGELEDA_STRINGS_H

#define MAX 255

#include <string.h>
#include <ctype.h>

void removeLineBreak(char arr[MAX]);
void removeLineBreakTXT(char arr[MAX]);

void lowercase(char arr[MAX]);

void stringDebug(char arr[MAX]);

#endif //FINALEDA_STRINGS_H
